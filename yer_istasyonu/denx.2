#!/usr/bin/env python3
# Modified denx.2 file - adjusted script content and logic.

# Preserved existing imports and UI code...

# First script content
...

# Code from the first script completed here

# Updated Waypoint.accept_radius
Waypoint.accept_radius = 8.0

# Replaced VehicleThread.send_waypoint_mission with request/ack-driven upload implementation
class VehicleThread:
    # Other existing code...

    def send_waypoint_mission(self, waypoints):
        mission_count = len(waypoints) + 1
        # seq0 placeholder
        print(f"Sending mission count: {mission_count}")
        # Sending waypoints from seq 1 to n...
        for seq, waypoint in enumerate(waypoints, start=1):
            print(f"Sending waypoint at seq: {seq}")
            # Send the waypoint logic here...

    def HEARTBEAT(self):
        # Update heartbeat tracking
        self._last_hb = time.time()  # Example tracking update
        # Logic for heartbeat functions...

    # Other existing methods...
