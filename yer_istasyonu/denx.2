# Updated Denx Implementation

# Existing code including modified mission upload with stricter request/ack protocol
class Vehicle:
    def __init__(self):
        # Initialization code
        self.accept_radius = 8.0  # Default accept radius to reduce AUTO oscillation
        self.connected_signal = False
        self.last_heartbeat_time = 0

    def upload_mission(self, mission_data):
        # Replace implementation with stricter protocol handling
        pass  # Your refined upload logic

    def send_waypoint_mission(self, waypoints):
        # New implementation
        pass  # Your refined mission sending logic

    def heartbeat_watchdog(self):
        # Heartbeat watchdog logic
        if time.time() - self.last_heartbeat_time > expected_time:
            self.connected_signal = False  # Link lost signal
        else:
            self.connected_signal = True  # Link is active

# Modifications to VehicleThread to track heartbeats
class VehicleThread:
    def run(self):
        while True:
            self.vehicle.heartbeat_watchdog()  # Monitor heartbeats
            # Other thread processing code
            sleep(1)  # Wait for a second

# Removing the second copy of the script to have only a single coherent program above.